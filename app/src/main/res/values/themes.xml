<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Customize your light theme here. -->
    <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/navy_blue</item>
        <item name="android:titleTextStyle">@style/AppTheme.TitleStyle</item>
        <!--Status Bar Transparent(스테이터스 바 투명으로 변경)-->
        <item name="android:statusBarColor">@color/white</item>
        <!--windowsTranslucentStatus 값은 Status Bar의 색에 대한 Indicator 값 -->
        <item name="android:windowLightStatusBar">true</item>
        <!--Navigation Bar의 색상을 투명하게 만드는 여부에 대한 Indicator 값-->
        <!--item name="android:windowTranslucentNavigation">true</item-->
    </style>

    <style name="AppTheme.TitleStyle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="SplashTheme" parent="Theme.AppCompat.Light">
        <!--Status Bar Transparent(스테이터스 바 투명으로 변경)-->
        <item name="android:statusBarColor">@color/white</item>
        <!--windowsTranslucentStatus 값은 Status Bar의 색에 대한 Indicator 값 -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@drawable/ic_background_splash</item>
    </style>
    <!--style name="Theme.MyApplication" parent="Base.Theme.MyApplication" /-->
</resources>